services:
  - type: web
    name: eeg-stress-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --worker-class gthread --timeout 300 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
